// -- mui -- //
import { Typography } from "@mui/material";

const Or = () => {
  return (
    <Typography
      align="center"
      variant="h5"
      component="p"
      sx={{ marginBottom: "1rem" }}
    >
      -- OR --
    </Typography>
  );
};

export default Or;
